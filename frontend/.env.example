# 実 Identity Platform テナントの Firebase Web config。実値は .env.local（gitignore）に置く。
# projectId はバックエンドの GCP_PROJECT_ID と一致させること（issuer を揃えるため）。
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
