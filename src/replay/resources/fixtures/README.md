# 実在馬フィクスチャ

studbook 繁殖 replay ハーネスの入力。1 ファイル = 実在馬 1 頭 × 1 繁殖シーズン。

- 各フィクスチャは公開記録（JAIRS 運営の JBIS-Search を第一の典拠とする）から手で起こす。
- `sourceUrl` に必ず出典 URL を記す。
- `manifest.txt` に読み込むファイル名を 1 行 1 件で列挙する（`#` 始まりはコメント）。
- 種付日が公開記録から厳密に取れない場合は、シーズンの代表日で近似し `sourceUrl` 脇にコメントで断る。
